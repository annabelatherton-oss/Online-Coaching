// The fixed calorie levels a coach can fork a plan group's 20-week meal selections for. A tier only
// shows up in that plan's editor once at least one of its active clients is assigned this exact
// calorie target — forking it copies the plan's standard meals as a starting point, so a coach only
// has to swap a meal once for everyone on that calorie level instead of editing every client.
export const CALORIE_TIERS = [1500, 1600, 1700, 1800, 1900, 2000]
